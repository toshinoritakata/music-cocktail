# Sound & Sip

音楽の音響特徴・ジャンル・ムード・BPMから、Jevが20種類のハウスレシピを選ぶローカルWebアプリです。マイク、音声ファイル、内蔵サンプルに対応します。

## 起動

Node.js 22以降。

```sh
npm ci
cp .env.example .env
# .env の TYPESAFE_API_KEY をローカルで設定（省略するとデモモード）
npm start
```

[アプリを開く](http://127.0.0.1:4317)。既存の `.env` がある場合はコピーで上書きしないでください。キーはGit管理対象外で、ブラウザには送信しません。設定変更後はサーバーを再起動します。

## 使い方

- 音楽ファイルを選んで再生するか「サンプル音を再生」を押します。
- 「マイクで聴く」でブラウザの許可を与えると、OS/ブラウザの既定マイクを使用します。入力選択UIは未実装です。停止、入力切替、ページ離脱時にマイクを解放します。マイク音はスピーカーへ出しません。
- 音響特徴は約5秒後、音楽タグはモデル準備後に12秒の音が集まると表示します。タグは約6秒ごとに再解析します。
- 5秒ごとの提案更新を切り替えられます。初回・手動以外は同じ候補が2回続くと更新。「固定」で気に入った一杯を保持します。
- 「ノンアルコールのみ」は候補をAPI呼出前に6種類へ絞ります。
- `samples/` に動作確認用の合成WAVを3種類同梱しています。`python3 samples/generate.py` で再生成できます。

## 解析と役割分担

1. Web AudioでRMS、スペクトル重心、低音比を測定し、直近10秒を平均。
2. WorkerでMSD MusiCNNを実行し、ジャンル・曲の印象を推定。TensorFlow.js WASM、Essentiaの前処理、16kHz monoを使用。
3. Essentia RhythmExtractor2013でBPMを推定。拍が不明瞭な場合は未判定にします。
4. Nodeサーバーが入力を検証し、推定タグ・BPMと音響特徴、レシピ候補をTypeSafeのJev Choiceへ送信。既存レシピのIDを選びます。

タグの数字はモデルスコアであり、正答率ではありません。弱い候補は「弱」と表示し、Jevにも暫定値として渡します。ムードは曲の印象で、聴取者の心理状態ではありません。BPMには半分・倍の誤推定があり、音量・環境音にも影響されます。30秒以上古いタグは使用しません。

音声ファイル・マイクの生音声は端末内で解析し、外部送信・録音保存しません。Jevに送信するのは集約した特徴・推定タグ・BPM・レシピ候補・選択条件です。APIキーはサーバー側だけで使用します。

キー未設定時は3つの音響特徴によるルール選択です（音楽タグは選択に使いません）。APIエラー時にデモへ黙って切り替えることはありません。説明欄は選択時の測定値と登録済み情報の表示であり、Jevの推論説明ではありません。

## 曲の評価軸

曲の良し悪しを採点するのではなく、カクテルに対応させるために次の6項目を使います。

| 項目 | 測定・推定内容 | Jevへ渡す値 | 味への対応のヒント |
|---|---|---|---|
| Energy | 波形のRMSによる音量感。曲の激しさそのものではない | `audio.energy`: 0〜1 | 活発さ、スパイス、炭酸 |
| Brightness | スペクトル重心による高域への偏り | `audio.brightness`: 0〜1 | 柑橘、シャープさ、爽快感 |
| Bass | 全周波数のパワーに占める250Hz未満の比率 | `audio.bass`: 0〜1 | コク、重厚さ、深み |
| ジャンル | MusiCNNの出力から対象28タグを抽出し、上位3候補を採用 | `music.genres`: ラベルと0〜1のスコア | jazz・bluesなら深み、など |
| ムード | MusiCNNの出力から曲の印象を表す8タグの上位3候補を採用 | `music.moods`: ラベルと0〜1のスコア | 穏やかなら丸い味、にぎやかなら華やかさ |
| BPM | Essentiaのビート解析によるテンポ | `music.bpm`: 整数、または未判定の`null` | 速いテンポなら軽快で爽やかな一杯、など |

音響特徴は約100msごとに計算し、直近10秒の平均を使います。Energyは `clamp((20 × log10(RMS) + 55) / 45)`、Brightnessは `clamp(log2(1 + 重心Hz / 400) / 4)` で0〜1に変換します。`clamp`は0未満を0、1超を1に制限する処理です。画面では100倍して表示します。録音音量やマスタリングに影響される相対的な指標です。

ジャンル・ムード・BPMは直近12秒を約6秒ごとに解析します。ムードの対象は `happy`（明るい）、`sad`（切ない）、`Mellow`（穏やか）、`chill`（落ち着く）、`chillout`（リラックス）、`party`（にぎやか）、`beautiful`（美しい）、`easy listening`（やさしい）です。画面では日本語に置き換えますが、Jevにはモデルの元ラベルを渡します。ジャンルの全28タグは [public/music-analysis.js](public/music-analysis.js) に定義しています。

タグスコアは校正された確率ではありません。0.15未満の候補も削除せず、弱い推定として渡します。BPMの探索範囲は40〜208で、解析器の信頼度が1未満、または検出拍が4個未満なら未判定にします。`tempoConfidence`には解析器の生の信頼度を渡します（0〜1の確率ではありません）。

## Jevへの渡し方

ブラウザからローカルサーバーの `POST /api/recommend` へ `features`、`music`、`preference` を送ります。サーバーは範囲とラベルを検証し、[catalog.mjs](catalog.mjs) の `requestBody()` でJevへのリクエストを組み立てます。[server.mjs](server.mjs) がサーバー側のAPIキーを使い、`POST https://api.typesafe.ai/v1/systemone` に送信します。

リクエストは次の3部分で構成します。

- **`state`：観測データと解釈上の注意。** `audio`に3つの音響特徴、`music`にモデル名・解析秒数・ジャンル・ムード・BPM・テンポ信頼度、`preference`にレシピ範囲を格納。`measurement`には「タグは確率ではない」「ムードは聴取者の感情ではない」「BPMには半分・倍の誤りがある」「弱いタグは暫定値」と明記します。
- **`questions.cocktail.instructions`：選択の指示。** 音の質感とタグを総合し、味への創作的な対応として選ぶよう指示します。表の対応例はヒントで、固定ルールや固定の数値重みではありません。弱いタグを重視しすぎず、`music`が`null`なら音響特徴だけを使います。
- **`questions.cocktail.criteria`：選択肢。** レシピIDをキーに、名前・味の説明・材料を渡します。通常は20種類、ノンアルコール指定では事前に6種類へ絞ります。配合を新しく生成するのではなく、既存の一杯を選ぶ`choice`質問です。

以下は構造を示すJSON例です。数値は説明用で、`measurement`と`instructions`は短縮しています。タグとレシピ候補も一部のみ掲載しています。

```json
{
  "model": "jev-latest",
  "state": {
    "audio": { "energy": 0.54, "brightness": 0.10, "bass": 0.99 },
    "music": {
      "source": "msd-musicnn-1",
      "windowSeconds": 12,
      "bpm": 119,
      "tempoConfidence": 2.5,
      "genres": [{ "label": "jazz", "score": 0.25 }],
      "moods": [{ "label": "chillout", "score": 0.05 }]
    },
    "measurement": "Tag activations are not calibrated probabilities; weak tags are tentative.",
    "preference": "all"
  },
  "questions": {
    "cocktail": {
      "type": "choice",
      "instructions": "Choose a pleasing metaphorical music-to-taste pairing. Weigh the combination. Avoid over-weighting weak tag scores.",
      "criteria": {
        "old-fashioned": {
          "name": "Old Fashioned",
          "taste": "温かみ、樽香、落ち着き",
          "ingredients": ["バーボン 45ml", "シロップ 5ml", "アロマティックビターズ 2滴"]
        }
      }
    }
  }
}
```

応答の `answers.cocktail.choice` を許可済みレシピIDと照合し、該当する配合・作り方を表示します。`answers.cocktail.confidence` は選択肢間の分布の集中度として表示し、正答率とは扱いません。レシピの `target`（3つの目標音響値）はAPIキー未設定のデモ用で、Jevの選択肢には渡していません。

## Jevがカクテルを決める基準

アプリ専用の追加学習は行っていません。毎回渡す「音と味の対応指示」と「候補レシピの定義」を使って、Jevに組み合わせを判断させています。生の音声をJevが聴く仕組みではなく、ブラウザで解析した特徴を入力します。利用履歴や好みを継続学習する機能もありません。

現在の `questions.cocktail.instructions` に含まれる対応のヒントは次のとおりです。これは開発側が置いた創作的な仮説で、科学的に検証済みの対応表ではありません。

| 音楽側の特徴 | 味・飲み心地の方向 |
|---|---|
| 高域が明るい | 柑橘、シャープさ |
| 低音が豊か | コク、深み |
| 音量感が強い | 活発さ、スパイス、炭酸 |
| `Mellow`・`chill` | 柔らかく丸い味 |
| `happy`・`party` | 明るく活発な味 |
| `blues`・`jazz` | 深み |
| 速いテンポ | 軽快で爽やかな一杯 |

すべてのジャンルやムードに個別の対応を定義しているわけではありません。表にないタグもスコアとともに渡し、候補レシピとの総合判断に使わせます。

カクテル側は、例えばオールドファッションドなら「温かみ、樽香、落ち着き」という味の説明と、バーボン・シロップ・ビターズの材料情報を定義しています（上のJSON例）。Jevは曲の特徴、対応のヒント、各候補の定義を照らし合わせ、許可されたレシピIDを1つ返します。

「低音が0.8以上なら必ずオールドファッションド」といった閾値や、6項目の固定の重み、明示的な合計点は設定していません。例えば「低音が豊かで穏やか」という入力から「深み・落ち着きのある一杯」を選ぶことを期待しますが、これは設計上の期待であり、Jevの内部推論を観測した説明ではありません。同じ特徴でも複数の候補が妥当になり得ます。

### 選択方法の有効性をどう評価するか

APIから有効なレシピが返ることは確認済みですが、人が感じる音と味の相性や、Jevが単純なルールより優れるかは未評価です。今後、同じ曲の同じ区間・同じ候補群を用いて、次の3方式を比較します。

1. **ルール選択**：現在のデモ方式。3つの音響特徴とレシピの目標値の距離で選択。
2. **音響特徴だけのJev**：`music: null` として、音量感・高域・低音だけから選択。
3. **全6項目のJev**：ジャンル・ムード・BPMも加えて選択。

方式名を隠し、提示順を入れ替えて「曲の印象に合うか」「その一杯を選びたいか」を人が評価します。レシピ説明だけを読んだ評価と、実際に試飲した評価は分けて記録します。あわせて連続区間での候補の安定性と待ち時間を測り、Jevを使う効果と音楽タグを加える効果を確認します。この比較実験はまだ実施していません。

## モデル比較

[CLMR・MERTの比較と評価計画](docs/model-comparison.md)。現在はMusiCNNを実装済み。CLMRとMERTは資料調査までで、導入・速度・精度の実測は未実施です。

## 検証

```sh
npm test
```

12件：入力と音楽タグの検証、Jevへの解析情報受渡し、ノンアルコール制約、マイク解放・権限競合、リサンプリングの帯域外抑制など。GitHub Actionsでも実行します。

ブラウザでは内蔵合成音からジャンル・ムード・約119 BPMを表示し、Jevの実APIによるレシピ選択まで確認しました。これは動作確認で、実音源による認識精度評価ではありません。

## 範囲と依存物

音と味の対応は創作的な解釈です。レシピ配合は試作用です。歌詞・配信サービス連携・在庫管理は未実装。127.0.0.1でのみ起動する試作で、外部公開向けの認証・利用制限はありません。

アプリ独自のコード・文書・合成サンプルは **AGPL-3.0-only** で公開します（[LICENSE](LICENSE)）。著作権・ライセンス表示を保持し、配布やネットワーク提供時には該当するソース提供条件を守ってください。無保証です。

同梱モデルは別ライセンスです。公式資料にCC BY-NC-SA 4.0とCC BY-NC-ND 4.0の表記揺れがあるため、ここでは改変せず非商用で再配布しています。商用利用・改変モデルの配布許可は保証しません。詳細は [THIRD_PARTY.md](THIRD_PARTY.md) と [公開前確認](docs/license-review.md) を参照してください。

[TypeSafe API公式仕様](https://docs.typesafe.ai/api)
